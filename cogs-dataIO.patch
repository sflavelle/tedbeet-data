*** /opt/docker-cfg/neubott/Red-DiscordBot/cogs/utils/dataIO.py	2018-02-01 22:49:41.414126339 +1100
--- /opt/docker-cfg/bucket/Red-DiscordBot/cogs/utils/dataIO.py	2018-02-11 23:01:08.107517046 +1100
***************
*** 16,30 ****
          path, ext = os.path.splitext(filename)
          tmp_file = "{}-{}.tmp".format(path, rnd)
          self._save_json(tmp_file, data)
!         try:
!             self._read_json(tmp_file)
!         except json.decoder.JSONDecodeError:
!             self.logger.exception("Attempted to write file {} but JSON "
!                                   "integrity check on tmp file has failed. "
!                                   "The original file is unaltered."
!                                   "".format(filename))
!             return False
!         os.replace(tmp_file, filename)
          return True
  
      def load_json(self, filename):
--- 16,22 ----
          path, ext = os.path.splitext(filename)
          tmp_file = "{}-{}.tmp".format(path, rnd)
          self._save_json(tmp_file, data)
!         self._save_json(filename, data)
          return True
  
      def load_json(self, filename):
